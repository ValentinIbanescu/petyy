<div class="container py-5">
  <div class="row">
    <div class="col-12 col-md-6 offset-lg-3">
      <div class="bg-white rounded p-3 shadow">
        <h3>Create a new pet</h3>
        <%= simple_form_for @pet do |f| %>
          <%= f.input :name, placeholder: 'Pet name' %>
          <%= f.input :age, placeholder: 'Pet age' %>
          <%= f.input :category, placeholder: 'Category' %>
          <%= f.input :medical_situation, placeholder: 'Medical situation' %>
          <%= f.input :diet, placeholder: 'Diet' %>
          <%= f.input :photo, as: :file%>
          <%= f.submit class: 'btn btn-flat' %>
        <% end %>
      </div>
    </div>
  </div>
</div>
